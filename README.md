# site-convertido-sass

Site responsivo feito com CSS puro convertido e componentizado para SCSS.

```
npm install -g sass
sass --watch css/main.scss:css/main.css
```

Creditos pelo site: Rocketseat.
